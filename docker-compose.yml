services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: monk-session-4-pgvector
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: monk
    ports:
      - "5432:5432"
    volumes:
      - monk-session-4-pgvector-data:/var/lib/postgresql/data
      - ./scripts/postgres-init.sql:/docker-entrypoint-initdb.d/00-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d monk"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  monk-session-4-pgvector-data:
